# Public panel only. The capture script masks visible e-mails and company names.
PANEL_URL=http://localhost:5678/webhook/mala-direta

# Optional: path to an already sanitized n8n canvas screenshot.
WORKFLOW_SCREENSHOT_SOURCE=
