{
  "name": "mala-direta-n8n-portfolio",
  "version": "2.0.0",
  "private": true,
  "description": "Case real de automacao de campanhas de e-mail com n8n, PostgreSQL e painel web.",
  "scripts": {
    "test": "npm run validate && npm run validate-docs && npm run security-scan",
    "validate": "node scripts/validate-public-workflows.js",
    "validate-docs": "node scripts/validate-doc-links.js",
    "security-scan": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/security-scan.ps1",
    "prepare-workflows": "node scripts/prepare-public-workflows.js",
    "screenshots": "node scripts/capture-portfolio-screenshots.js"
  },
  "devDependencies": {
    "puppeteer": "^24.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
